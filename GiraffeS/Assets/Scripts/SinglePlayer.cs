using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class SinglePlayer : MonoBehaviour
{
    //Only when in the menu scene
    //Disable or enable the game objects and components for the top giraffe in Axel's scene and the size and stuff for the bottom camera.
    //Disable the second giraffe in the menu scene
    //and make the giraffe work with both axes and both(or all 3) jump buttons


    void Start()
    {
        
    }

    void Update()
    {
        
    }
}
